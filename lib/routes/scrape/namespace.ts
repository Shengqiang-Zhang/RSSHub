import type { Namespace } from '@/types';

export const namespace: Namespace = {
    name: 'Generic Scraper',
    url: 'github.com/Shengqiang-Zhang/RSSHub',
    description: 'Custom HTML-to-RSS transformer exposed via query parameters so it survives proxies that normalise `%2F` in paths (e.g. Azure App Service).',
    lang: 'en',
};
